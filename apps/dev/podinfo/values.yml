---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: podinfo
  namespace: apps
spec:
  chart:
    spec:
      version: 6.5.1
  values:
    replicaCount: 3
    service:
      type: LoadBalancer
